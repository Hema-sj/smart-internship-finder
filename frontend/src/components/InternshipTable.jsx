import InternshipCard from './InternshipCard';
export default function InternshipTable({ internships, title = 'Recommended Internships' }) { return <section><div className="flex items-end justify-between"><h2 className="text-2xl font-bold">{title}</h2><span className="text-sm text-slate-500">{internships.length} results</span></div>{internships.length ? <div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{internships.map(internship => <InternshipCard key={internship.id} internship={internship} />)}</div> : <p className="mt-5 rounded-xl bg-white p-6 text-sm text-slate-500">No internships match these filters.</p>}</section>; }
