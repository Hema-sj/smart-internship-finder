import { GraduationCap } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';

export default function Header() { const { user, logout } = useAuth(); const portalPath = user?.role === 'company' ? '/company/portal' : user?.role === 'admin' ? '/admin/portal' : '/dashboard'; return <header className="border-b border-slate-200 bg-white"><div className="mx-auto flex h-16 max-w-6xl items-center px-6"><Link to="/" className="flex items-center gap-2 font-bold text-emerald-700"><GraduationCap size={24} /> Smart Internship Finder</Link><div className="ml-auto flex items-center gap-4 text-sm">{user ? <><Link className="font-medium text-slate-600" to={portalPath}>{user.role === 'student' ? 'Dashboard' : 'Portal'}</Link><button onClick={logout} className="font-semibold text-emerald-700">Log out</button></> : <><Link className="text-slate-600" to="/login">Student login</Link><Link className="rounded-md bg-emerald-700 px-3 py-2 font-semibold text-white" to="/register">Create account</Link></>}</div></div></header>; }
