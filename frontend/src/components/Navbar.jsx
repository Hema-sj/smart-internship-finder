import { Bell, GraduationCap, UserRound } from 'lucide-react';
import { Link, NavLink } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';

const links = [['Home', '/'], ['Internships', '/internships'], ['Locations', '/locations']];
export default function Navbar() { const { user, logout } = useAuth(); return <header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl flex-wrap items-center gap-5 px-5 py-3"><Link className="flex shrink-0 items-center gap-2 font-bold text-emerald-700" to="/"><GraduationCap size={25}/> Smart Internship Finder</Link><nav className="order-3 flex w-full gap-4 overflow-x-auto border-t border-slate-100 pt-3 text-xs font-semibold text-slate-600 xl:order-2 xl:w-auto xl:flex-1 xl:border-0 xl:pt-0">{links.map(([label, to]) => <NavLink className={({isActive}) => `whitespace-nowrap ${isActive ? 'text-emerald-700' : 'hover:text-emerald-700'}`} key={to} to={to}>{label}</NavLink>)}</nav><div className="ml-auto flex shrink-0 items-center gap-3">{user ? <><Link aria-label="Notifications" className="text-slate-600" to="/notifications"><Bell size={19}/></Link><Link aria-label="Student Profile" className="flex items-center gap-1.5 rounded-md bg-emerald-50 px-2.5 py-2 text-xs font-semibold text-emerald-800" to="/profile"><UserRound size={16}/>{user.name?.split(' ')[0]}</Link><button onClick={logout} className="text-xs font-semibold text-emerald-700">Logout</button></> : <Link className="rounded-md bg-emerald-700 px-3 py-2 text-xs font-semibold text-white" to="/login">Login</Link>}</div></div></header>; }
