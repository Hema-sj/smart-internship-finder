import { Building2, MapPin } from 'lucide-react';
export default function InternshipCard({ internship }) { return <article className="rounded-xl border border-slate-200 bg-white p-5"><Building2 className="text-emerald-700"/><h3 className="mt-4 font-bold">{internship.title}</h3><p className="mt-1 text-sm text-slate-600">{internship.company}</p><div className="mt-5 flex items-center justify-between text-xs"><span className="inline-flex items-center gap-1 text-slate-500"><MapPin size={13}/>{internship.location}</span><span className={internship.type === 'Paid' ? 'font-bold text-emerald-700' : 'font-bold text-amber-700'}>{internship.type}</span></div><p className="mt-3 text-sm font-semibold">{internship.stipend}</p><div className="mt-4 flex flex-wrap gap-2">{internship.skills.map(skill => <span key={skill} className="rounded-full bg-emerald-50 px-2 py-1 text-xs text-emerald-800">{skill}</span>)}</div></article>; }
